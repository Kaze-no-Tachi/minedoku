name: minedoku_engine
description: "Pure-Dart puzzle engine for Minedoku (rules, solver, generator, hints)."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
